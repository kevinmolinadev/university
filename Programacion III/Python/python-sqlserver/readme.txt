# Pasos para que el programa funcione

1.Primero debe crear la base  de datos en SQL Server

    CREATE DATABASE concierto

2.Una vez creada la base de datos en sql ingrese a la base de datos concierto, copie la consulta para crear las tablas, la consulta esta
dentro de la carpeta db 

3.Luego debe conectarse a su servidor SQL Server, puede hacer la conexion dentro del archivo conexion_sql.py dentro del archivo hay instrucciones par una conexion exitosa

Eso seria todo y estaria todo listo para probar el sistema